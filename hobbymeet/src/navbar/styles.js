const styles = (theme) => ({
    root: {
        flexGrow: 1
    },
   
    title: {
        flexGrow: 1,
        marginLeft: theme.spacing(2),
        marginRight: theme.spacing(2),
    },
    button: {
        marginLeft: "auto",
        marginRight: theme.spacing(2),

    }
});

export default styles;